<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ControlTemplate x:Key="ValidatedControlStyle.ErrorTemplate">
        <StackPanel Orientation="Horizontal">
            <AdornedElementPlaceholder />
            <Image Margin="2,0,0,0" Source="ValidationError.png" />
        </StackPanel>
    </ControlTemplate>
    <Style x:Key="ValidatedControlStyle" TargetType="Control">
        <Setter Property="Validation.ErrorTemplate" Value="{StaticResource ValidatedControlStyle.ErrorTemplate}" />
        <Style.Triggers>
            <Trigger Property="Validation.HasError" Value="true">
                <Setter Property="ToolTip" Value="{Binding Path=(Validation.Errors)[0].ErrorContent,RelativeSource={x:Static RelativeSource.Self}}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style BasedOn="{StaticResource ValidatedControlStyle}" TargetType="TextBox"></Style>
    <Style BasedOn="{StaticResource ValidatedControlStyle}" TargetType="ComboBox"></Style>

</ResourceDictionary>